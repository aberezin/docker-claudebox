name: python
description: Python code intelligence — the pyright-lsp Claude plugin (pyright server baked into the image).
requires-bake: true
# See features/go/manifest.yml for the requires-bake semantics.
